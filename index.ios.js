/**
 * Sample React Native App
 * https://github.com/facebook/react-native
 * @flow
 */

import React, { Component } from 'react';
import {
    AppRegistry,
    StyleSheet,
    Text,
    View
} from 'react-native';
let app = require("./js/App");
AppRegistry.registerComponent('redux_with_react_navigation', () => app);